/*
 * TabContainer Messages
 *
 * This contains all the text for the TabContainer component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.TabContainer';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the TabContainer component!',
  },
});
