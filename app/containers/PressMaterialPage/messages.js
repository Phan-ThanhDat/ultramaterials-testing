/*
 * PressMaterialPage Messages
 *
 * This contains all the text for the PressMaterialPage container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.PressMaterialPage';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the PressMaterialPage container!',
  },
});
