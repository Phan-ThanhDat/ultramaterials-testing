/*
 * VolunteeringPage Messages
 *
 * This contains all the text for the VolunteeringPage container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.VolunteeringPage';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the VolunteeringPage container!',
  },
});
