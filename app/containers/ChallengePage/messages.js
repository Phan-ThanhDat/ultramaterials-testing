/*
 * ChallengePage Messages
 *
 * This contains all the text for the ChallengePage container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.ChallengePage';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the ChallengePage container!',
  },
});
